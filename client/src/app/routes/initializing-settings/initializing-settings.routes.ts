import { Routes } from '@angular/router';
import LocalizationStringComponent from 'app/project/initialization/localization-string/localization-string.component';

export const routes: Routes = [
  { path: 'localization-string', component: LocalizationStringComponent }
];
