import { Routes } from '@angular/router';
import OperationPageComponent from 'app/project/operation/operation-page/operation-page.component';

export const routes: Routes = [
  { path: 'operationPage', component: OperationPageComponent }
];
