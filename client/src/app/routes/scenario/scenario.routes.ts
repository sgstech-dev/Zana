import { Routes } from '@angular/router';
import MapComponent from 'app/project/scenario/map/map.component';

export const routes: Routes = [
  { path: 'designBattlefield', component: MapComponent }
];
