import { Routes } from '@angular/router';
import { SensorPageComponent } from 'app/project/sensors/sensor-page/sensor-page.component';

export const routes: Routes = [
  { path: 'sensorPage', component: SensorPageComponent }
];
